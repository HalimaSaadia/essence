

const SectionHeading = ({text}) => {
    return (
        <>
            <h1 className="text-2xl logo-font font-bold text-[#010E0E]">{text}</h1>
        </>
    );
};

export default SectionHeading;